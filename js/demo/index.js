   // this is for javascript
        // allows for new ES6 features
        "use strict;"
        function printToNumber(number) {
            for(var i = 0; i <= number; i++) {
                console.log(i);
            }
        }

        printToNumber(10);